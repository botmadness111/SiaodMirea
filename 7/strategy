Я должен запуустить дерево решений

если к корню возвращается хоть один True -> end

от корня мы запускаем ветки на каждую клетку следующего ряда

после этого каждая такая клетка является корнем. Которая возвращает true || false


Изначально у меня есть пустая доска
	1)Я ставлю Q на одну из клеток первого ряда - эта клетка и будет главным корнем
	2)от корня запускаю ту же функцию - т.е. создаю новый корень но на ряд ниже
	3)если превзошли границы и не поставили Q - return false ИНАЧЕ return true